import React, { useState } from "react";

export const Navbar = () => {
  return (
    <div style={{display: "flex", alignItems:"center", justifyContent: "space-between", margin: "1rem 2rem"}}>
      <button>Camisa Vines</button>

      <div>
        <p>switcher</p>
      </div>
    </div>
  );
};
